import React from "react";

const Home = ({ authenticated, name, email }) => {
  //  const { name, email } = currentUser;
  return (
    <div>
      <div>홈이다</div>
    </div>
  );
};

export default Home;
